export const metadata = {
  title: "Login | Bezar",
  description: "Securely sign in to your Bezar OTT account using password-less magic links.",
};

export default function LoginLayout({ children }) {
  return children;
}
